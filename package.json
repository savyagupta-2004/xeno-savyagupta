{
  "scripts": {
    "dev": "node simple-server.js",
    "start": "node simple-server.js",
    "build": "npx prisma generate",
    "vercel-build": "npx prisma generate && echo 'Build completed'",
    "postinstall": "prisma generate"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "axios": "^1.6.2",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.7",
    "prisma": "^5.7.0"
  },
  "devDependencies": {
    "eslint": "^9.35.0"
  }
}
