# Railway deployment configuration
PORT=$PORT
NODE_ENV=production

# Database (Railway PostgreSQL)
DATABASE_URL=$DATABASE_URL

# JWT Configuration  
JWT_SECRET=$JWT_SECRET
JWT_EXPIRES_IN=7d

# Email Configuration
EMAIL_APP_PASSWORD=$EMAIL_APP_PASSWORD

# No Vercel protection needed on Railway!